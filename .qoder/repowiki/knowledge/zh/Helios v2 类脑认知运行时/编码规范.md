- 每个认知模块严格遵循 `engine.py` (实现逻辑) 与 `contracts.py` (数据契约/协议) 分离的结构。
- 所有跨模块状态传递均通过冻结的 dataclass (如 `RuntimeFrame`, `StimulusBatch`) 实现，确保不可变性。
- 运行时阶段适配器 (`stages.py`) 仅负责桥接所有者 API 与内核，不持有认知策略或语义判断。
- 关键依赖缺失时强制触发 `RuntimeStartupError` 或 `CompositionError`，禁止静默 fallback 或降级行为。