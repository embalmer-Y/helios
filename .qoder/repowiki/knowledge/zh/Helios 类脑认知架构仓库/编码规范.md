- 所有暴露跨模块接口的模块必须在文档字符串中声明“Owner”、“Owns”及“Does not own”边界。
- 公共 API 方法必须包含标准化的 docstring，明确说明 Owner、Purpose、Inputs、Returns 及 Raises。
- 跨执行边界或需审计的交互必须使用冻结数据类（frozen dataclass）定义的操作合约（Ops Contract），而非通用字典。