# Helios v2 Requirements Index

| Req | Name | Status | Maturity | Depends On | Notes |
| --- | --- | --- | --- | --- | --- |
| 01 | Runtime Kernel | draft | relatively_complete | none | Establishes startup gating, lifecycle orchestration, and owner-safe stage dispatch |
| 02 | Sensory Ingress | draft | relatively_complete | 01 | Establishes owned stimulus normalization, source registration, and ingress API/ops contracts |
| 03 | Rapid Salience Appraisal | draft | baseline_implementation | 02 | Establishes fast-path coarse salience appraisal and the sensory-ingress to appraisal API/ops boundary |
| 04 | Neuromodulator System | draft | baseline_implementation | 03 | Establishes independently modeled neuromodulator state contracts, modulation update API/ops, and explicit confirmation gates for learned update semantics |
| 05 | Interoceptive Feeling Layer | draft | baseline_implementation | 04 | Establishes subjective body-feeling state from neuromodulator state and normalized internal signals, with soft-modulation-only output contracts |
| 06 | Memory Affect and Replay | draft | baseline_implementation | 05 | Establishes affect-tagged memory snapshots, replay-candidate surfacing, forced consolidation constraints, and candidate memory supply to later workspace owners. With R45 the `06` formation is de-shimmed under the semantic-memory assembly (affect-tagged from the real `05` feeling state) and consolidation worth is decided by an owner-owned salience gate; consolidation-worthy memory is durably persisted and semantically recalled. With R69 semantic assembly is the default, so `03`–`06` inputs are real by default. Still baseline: content shaping is minimal, dedup/merge deferred |
| 07 | Workspace Competition and Working State | draft | baseline_implementation | 06 | Establishes candidate-set competition, short-lived working-state ownership, and workspace-facing promotion boundaries for memory-derived content. With R46 the `07` competition is de-shimmed under the semantic-memory assembly (real score from the `06` `priority_hint` + the real `05` feeling salience) and the working state becomes a real bounded attention bottleneck (top-K retention; a consolidated `06` candidate may lose the attention competition and not be held this tick). Still baseline: first-version weights/bound, single-source candidates |
| 08 | Reportable Conscious Content | draft | relatively_complete | 07 | Establishes conscious-content commitment from workspace outputs, explicit no-commit semantics, and a non-reach-through upstream content-material boundary. With R47 the commitment focal-selection is de-shimmed under the semantic-memory assembly: `08` ignites the single highest-`workspace_score_hint` retained candidate as focal reportable content (global-workspace winner-take-all) and demotes the rest to bounded supporting context, instead of declaring `semantic_conflict_unresolved` on retained multiplicity |
| 09 | Thought Gating and Continuation Pressure | draft | baseline_implementation | 08 | Establishes the sole owner of thought-window firing decisions, compact gate observability, and multi-tick continuation-pressure carry without collapsing into retrieval or thought generation. With R37 the gate consumes the real `04` norepinephrine as `neuromodulatory_arousal`; with R48 the gate's `global_activation_level` is grounded in the real `07` workspace activation; with R53 `workload_pressure` reads the real interoceptive afferent; with R55 `temporal_signal` and `dmn_available` come from the real `helios_v2.temporal` owner; with R62 `drive_urgency_signal` carries the real prior-tick `18` outward drive; with R63 `selected_stimuli` projects the real `03` appraisal. After R63 every gate input is real; no constant shim remains in the gate signal. Still baseline: P5 coefficient learning and cross-channel coupling deferred |
| 10 | Directed Retrieval Into Thought Window | draft | baseline_implementation | 09 | Establishes the sole owner of retrieval-query planning, tiered memory selection, and bounded thought-window bundle assembly without collapsing retrieval policy back into memory ownership. With R33/R34 the candidate providers are real (recency, then semantic); with R49 the request's `recall_intent`/`selected_memory_refs` come from the prior tick's `11` `MemoryHandoffDirective` (memory-guided retrieval), with a stimulus-driven absence fallback, under the semantic-memory assembly |
| 11 | Internal Thought Loop Owner | draft | baseline_implementation | 10 | Establishes the sole owner of fired-path thought execution, sufficiency and continuation judgment, and proposal emission without collapsing into persistence, planning, or governance acceptance |
| 12 | Action Proposal and Externalization Contract | draft | baseline_implementation | 11 | Establishes the sole owner of thought-origin proposal normalization, externalization contract publication, and bridge-level rejection semantics without collapsing into planner acceptance or executor dispatch |
| 13 | Planner Executor Feedback Bridge | draft | baseline_implementation | 12 | Establishes the sole owner of proposal-to-decision bridging, formal rejection and execution-outcome publication, and normalized bridge feedback semantics without collapsing into transport or feedback persistence |
| 14 | Identity Governance and Self Revision Integration | draft | baseline_implementation | 13 | Establishes the sole owner of self-revision governance, identity-state mutation, proactive governance pressure, and formal revision-result publication without collapsing into thought generation, personality projection, or audit persistence |
| 15 | Execution Writeback and Autobiographical Consolidation | draft | baseline_implementation | 13, 14 | Establishes the sole owner of execution-result writeback, autobiographical/semantic consolidation handoff, and continuity-preserving experience publication without collapsing into planner, governance, or raw storage backends |
| 16 | Embodied Subjective Prompt and Action Autonomy | draft | baseline_implementation | 11, 12, 13, 15 | Establishes the sole owner of embodied subjective prompt-contract assembly for thought and outward expression without collapsing into thought ownership, planner authority, or identity governance |
| 17 | Evaluation Fidelity and Diagnostic Provenance | draft | baseline_implementation | 13, 15, 16 | Establishes the sole owner of evidence-driven runtime evaluation and diagnostic provenance publication without collapsing into runtime mutation or planner/channel shortcuts |
| 18 | Subjective Autonomy and Proactive Evolution | draft | relatively_complete | 09, 10, 11, 13, 15, 16, 17 | Establishes the sole owner of proactive-drive integration, multi-tick self-directed continuation, long-horizon deferred-continuity decay/merge/resolution, and controlled proactive externalization without collapsing into prompt theater or channel-owned triggering |
| 19 | Architecture Boundary and Owner Documentation | draft | baseline_implementation | 01-18 | Establishes the formal v2 boundary-truth documentation set that keeps runtime owners, allowed dependencies, migration notes, and prohibited shortcuts aligned with implementation truth |
| 20 | Brain Architecture Comparison and Scientific Grounding | draft | baseline_implementation | 19 | Establishes the formal scientific-grounding documentation set that maps v2 owner domains to cautious brain-function analogs, evidence categories, explicit non-goals, requirement-linked gap analysis, and an owner-wave gap roadmap |
| 21 | Unified Runtime Observability and Logging | draft | baseline_implementation | 01 | Establishes the sole owner of structured runtime log events, severity/event-kind taxonomies, fail-fast sink dispatch, and an optional kernel-level emission seam that produces a correlated per-tick stage timeline across the `01-18` chain without becoming authoritative state or a cross-owner reach-through channel |
| 22 | Runtime Composition Root and Runnable Runtime | draft | baseline_implementation | 01-18, 21 | Establishes the sole assembly owner that wires the dependency gate, the canonical nineteen-stage chain with shipped first-version owner-neutral bridges, and an optional observability recorder into a single runnable runtime handle plus a thin driver, and enforces the `21` owner as the single logging mechanism, without owning cognitive policy or introducing any degraded assembly path |
| 23 | Execution-Timeline-Aware Evaluation and Consequence Binding | draft | baseline_implementation | 17, 21, 22 | Establishes observability-owned read-only execution-timeline reconstruction and upgrades the evaluation owner to consume the prior-tick timeline as formal evidence and publish consequence-binding scores distinguishing internally-activated, blocked, rejected, executed, and continuity-written outcomes, without the log channel becoming an authoritative decision transport |
| 24 | Long-Horizon Continuity Threads and Reinforcement | draft | baseline_implementation | 18, 22, 23 | Establishes a first-class continuity-thread concept in the autonomy owner that aggregates deferred continuity across ticks into reinforceable, conflict-arbitrated, age-aware long-horizon threads and publishes an owner-owned long-horizon continuity state to the read-only evaluation owner, building on the existing decay/merge/expire semantics without inventing motive content or moving continuity ownership out of `18` |
| 25 | LLM Inference Gateway | draft | baseline_implementation | 01, 22 | Establishes the sole backend-neutral LLM inference capability owner: neutral request/completion contracts, a named-profile registry for per-consumer model/endpoint binding, provider dispatch behind an injected provider protocol, deterministic network-free static readiness wired into the startup dependency gate, and an opt-in live readiness probe, without owning prompt assembly, cognitive interpretation of model output, or cross-owner state transport |
| 26 | LLM-Backed Internal Thought | draft | baseline_implementation | 11, 22, 25 | Establishes the first real cognition consumer: an LLM-backed `InternalThoughtPath` that sources thought content from the `25` gateway through a neutral request while the `11` owner retains all sufficiency/continuation/proposal judgment, binds the thought consumer to a named profile as a fail-fast critical dependency, and fails fast on inference failure without silently falling back to deterministic synthesis, changing no `11` result contract |
| 27 | Structured Thought Output Driving Owner Judgment | draft | baseline_implementation | 25, 26, 22 | Upgrades the LLM-backed thought path to request a structured `json_object` thought envelope (sufficiency/continuation/action/self-revision intent) that the `11` owner parses, bounds, and maps into its final fired-cycle judgment, so real cognition measurably influences sufficiency, continuation, and proposal decisions while the owner remains sole judgment authority; fails fast on malformed structured output without retrieval-only fallback, changes no `11` result contract, and adds an explicit deterministic offline driver path. Surfaced the internal-only tick closure boundary, deferred to wave_C / `28` |
| 28 | Internal-Only Tick Closure (wave_C opener) | draft | baseline_implementation | 27, 13, 15, 18, 17 | Closes the internal-only tick: a fired tick that produces no externalizable proposal (a model continue/no-action decision) completes through the planner-bridge (`no_actionable_proposal`), writeback (`internal_only` continuity), autonomy, and evaluation (`internal_only_decision`) owners as an explicit internal-only outcome recorded through formal owner contracts, without fabricating an action, without weakening the externalizing path, and without granting outward channel execution authority |
| 29 | Cognition-Derived Autonomy Drive Inputs | draft | baseline_implementation | 26, 27, 28, 18, 24 | Replaces the hardcoded autonomy drive-input constants in the composition bridge with values derived from the thought owner's real fired-cycle decision plus the planner and continuation results, so the proactive disposition becomes a faithful downstream consequence of genuine cognition and the `24` continuity-thread layer forms/reinforces on real deferrals (previously permanently inert), while the `18` owner remains the sole disposition authority and no outward channel execution authority is added |
| 30 | Channel Driver Subsystem Framework | draft | baseline_implementation | 02, 13, 25 | Establishes the channel transport owner as a Linux-kernel-driver-style subsystem: a thin framework (driver registry + tick-boundary schedulers) with one uniform `ChannelDriver` protocol, runtime-pluggable register/deregister, NAPI-style bounded inbound drain emitting QoS-tagged `RawSignal` objects into sensory, bounded outbound dispatch of planner-accepted decisions, transport-intrinsic QoS marking (salience stays with `03`), real per-driver channel state for the planner, and fail-fast driver readiness; owns transport only, not normalization/salience/selection/content-shaping. Framework + deterministic fake driver shipped; QoS rides the `channel_qos` metadata reserved key (sensory unchanged); not wired into the assembled runtime (binding deferred to `31`) |
| 31 | CLI Channel Driver | draft | baseline_implementation | 30, 02, 13, 22, 28 | Implements the first concrete `ChannelDriver` (local CLI, bidirectional, no external side effects) conforming to the `30` protocol, and wires the channel subsystem into the runtime through an explicit opt-in assembly seam (inbound drain stage feeding sensory, outbound dispatch stage after planner, real planner channel-state) so a local operator round trip works, while keeping the default test assembly network-free and introducing no cognitive ownership in the driver. Shipped: `CliChannelDriver` with injected in-memory I/O; the two transport stages; `assemble_runtime(channel_cli=True)` opt-in 21-stage variant; subsystem-backed sensory source + real planner channel-state provider + `channel_drivers_ready` gate; `--channel-cli` local entry point. Default 19-stage assembly unchanged; full suite green and network-free |
| 32 | Execution-Truth-Corroborated Consequence Binding | draft | baseline_implementation | 17, 21, 22, 23 | Closes `wave_A_behavioral_truth`: the evaluation owner now corroborates the previous completed tick's self-reported consequence outcome against that same tick's kernel execution timeline and publishes a first-class corroboration verdict (`corroborated` / `discrepant` / `unverifiable_no_timeline`), escalating any contradiction into a dedicated `consequence_discrepancy` fidelity warning. Evaluation publishes a per-tick `ConsequenceClaim`; composition carries the prior tick's claim forward tick-aligned with its timeline through an owner-neutral bridge. Read-only, no scoring redesign (the existing outcome taxonomy and `internal_to_visible_consequence` scoring are unchanged), no fallback (absence/tick-mismatch is `unverifiable`, never optimistic), and no new logging mechanism. Default and channel-bound assemblies both carry the claim; full suite green and network-free |
| 33 | Durable Experience Store and Restart Continuity | draft | baseline_implementation | 15, 10, 22 | P2 opener. Establishes the durable experience-store owner (`helios_v2.persistence`): a `PersistedExperienceRecord` contract, an injected `ExperienceStoreBackend` protocol with a first-version SQLite file backend plus a deterministic in-memory double, an `ExperienceStore` facade (append / recent-N / count / prior-existence snapshot), and a store-backed directed-retrieval candidate provider. An owner-neutral composition seam appends each tick's `15` continuity records (provenance preserved verbatim) after the tick, mirroring the timeline carry; on an opt-in `assemble_runtime(experience_store=...)` the store-backed provider replaces the `10` fabricating shim so persisted experience re-enters the thought window, and after a process restart against the same SQLite file the prior session's experience is retrieved (deterministic recency/provenance; no embeddings/vectors — that is R34). Opt-in and default-off (default assembly byte-for-byte unchanged); `experience_store_ready` critical dependency fails fast when the backend cannot initialize; no degraded non-persistent path; no new logging mechanism; store file git-ignored. Full suite green and network-free |
| 34 | Semantic Experience Retrieval | draft | baseline_implementation | 33, 10, 25 | P2 second slice (P2→P3 hinge). Establishes the backend-neutral embedding capability owner (`helios_v2.embedding`, mirroring `25`): neutral request/result contracts, named-profile registry, injected `EmbeddingProvider` protocol with a lazy OpenAI-compatible provider plus a deterministic fake, a fail-fast `EmbeddingGateway`, network-free static readiness, and an opt-in live probe. `PersistedExperienceRecord` gains an optional embedding vector (additive) that both backends persist/read back exactly across a SQLite re-open; the store gains a deterministic bounded cosine `search_similar` (ranks only embedded records, reports skipped non-embedded, explicit tie-break, no external vector index/numpy) plus a `SemanticStoreBackedDirectedMemoryCandidateProvider`. On an opt-in `assemble_runtime(experience_store=..., embedding_gateway=...)` records are embedded at write and `10` recalls by similarity to the retrieval query (semantic recall across restart), replacing recency-only selection; the persistence owner never imports the embedding owner (query-embedding is injected). Semantic memory requires durable persistence (else `CompositionError`); `embedding_profile_ready` fails fast on an unready profile; embedding failure is a hard stop with no recency fallback. `03` appraisal unchanged (novelty-from-memory is the first P3 slice). Default, recency-only, and channel-bound assemblies unchanged when off; no new heavyweight dependency; full suite green and network-free |
| 35 | Memory-Grounded Novelty Appraisal | draft | baseline_implementation | 03, 33, 34 | P3 first cognitive-owner de-shim. The `03` appraisal owner's `novelty` dimension becomes a real signal: `novelty = clamp(1 - max_similarity, 0, 1)`, where `max_similarity` is the embedded stimulus's top cosine similarity to stored experience (via the `34` embedding gateway + `33` store `search_similar`). `03` defines a narrow `MemorySimilaritySource` protocol and owns the novelty salience mapping in `MemoryGroundedDimensionEstimator`; composition injects an owner-neutral `MemoryGroundedSimilaritySource` (returns the raw cosine retrieval fact or `None`), so `03` imports neither the embedding nor persistence owner. The capability triggers on the existing semantic-memory opt-in (store + embedding gateway both present). Empty stimulus content and a cold/all-non-embedded store yield the defined maximum novelty `1.0` (no gateway call for empty content); a runtime embedding/store failure is a hard stop with no constant fallback. The first-version comparison is cross-register (stimulus input vs `15` result summaries sharing the embedding profile), explicitly noted and not over-claimed; method B (persist the raw stimulus stream) is a future slice. The other four dimensions and the aggregate estimator are unchanged (later P3 slices). Default and recency-only assemblies keep constant novelty `0.6`; full suite green and network-free |
| 36 | Appraisal-Derived Neuromodulation | draft | baseline_implementation | 03, 04, 35 | P3 second cognitive-owner de-shim. The `04` neuromodulator update path becomes a deterministic appraisal-derived dynamics model: levels = `clamp(tonic_baseline + sum(sensitivity_k * salience_k), legal_min, legal_max)` per channel, with reward(+weak novelty)→dopamine, novelty+uncertainty→norepinephrine, threat→cortisol, and other channels regressing to the tonic baseline. The batch is aggregated per-dimension max across appraisals (winner-takes-attention); an empty batch yields the tonic baseline. `AppraisalDerivedNeuromodulatorUpdatePath` lives in composition `bridges.py` conforming to the owner's existing `NeuromodulatorUpdatePath` protocol, so the `04` engine is unchanged; it reads only the batch + config (no other owner's state, no prior-tick levels). Triggers on the same semantic-memory opt-in as R35 (deriving neuromodulation from appraisal only matters once appraisal is real). Stateless this slice: dual-timescale tonic/phasic decay (prior-tick carry) is deferred. Sensitivity coefficients are explicit bounded first-version constants under the config's declared learned-parameter categories (P5-learnable later); the mapping is a linear combination plus clamp, no NN, no divergence. Default/recency/offline assemblies keep the constant levels; full suite green and network-free |
| 37 | Neuromodulatory Gating Coupling | draft | baseline_implementation | 04, 09, 36 | P3 third cognitive-owner de-shim, and the first time a real `04` level shapes a downstream cognitive decision. The `09` `ThoughtGateSignalSnapshot` gains one additive optional raw fact `neuromodulatory_arousal: float | None` (`[0,1]`, default `None`); composition's `NeuromodulatorAwareThoughtGateSignalBridge` reads the `04` `NeuromodulatorStageResult` already present in the frame and forwards `levels.norepinephrine` verbatim (no mapping). The `09` owner gains an `ArousalAwareThoughtGatePath` that owns the arousal-to-gate mapping: when the fact is present it adds one non-negative bounded term `arousal_gain * arousal` (first-version `arousal_gain = 0.15`, under the `gate_policy` learned-parameter category) to the gate score and records it in `contributing_signals`; the existing `global_activation_level` term and all other terms are untouched (de-shimming that constant is a separate later slice). The coupling is monotonic, deterministic, stateless (no prior-tick read), and structurally never a hard gate: weight `0.15 < fire_threshold 0.55` so arousal alone cannot force a fire, and being additive non-negative it cannot suppress a fire other signals justify. Triggers on the same semantic-memory opt-in as R35/R36; `None` reproduces the first-version path byte-for-byte, so default/recency/offline assemblies are unchanged. Cortisol/inhibition hard-gate coupling and `04`→`05` feeling are deferred. No new logging mechanism; full suite green and network-free |
| 38 | Neuromodulator-Derived Feeling | draft | baseline_implementation | 04, 05, 36 | P3 fourth cognitive-owner de-shim, bringing `04`'s second downstream consumer to real so both `09` (R37) and `05` now consume the real `04` state. The constant `FirstVersionFeelingConstructionPath` (which ignored its `neuromodulator_state` argument and returned a fixed 7-dim vector) is replaced under the semantic-memory assembly by an owner-private `NeuromodulatorDerivedFeelingConstructionPath` in `helios_v2.feeling` (the `05` engine and contracts are unchanged; no new bridge, no stage reorder — `05` already receives the full `04` `NeuromodulatorState`). Each feeling dimension = `clamp(baseline_dim + sum(coupling_k * level_k), legal_min, legal_max)`: valence +DA/opioid/5-HT −cortisol; arousal +NE/excitation; tension +cortisol/NE; comfort +opioid/oxytocin/5-HT −cortisol; pain_like +cortisol −opioid; social_safety +oxytocin/5-HT −cortisol; fatigue +inhibition −excitation (weak). The mapping (feeling subjectivation) is owned by `05` itself, deterministic, bounded (no NN, clamped to legal range), and stateless (no prior-tick feeling read; dual-timescale `feeling_persistence` deferred). Coupling coefficients are explicit bounded first-version constants under the config's declared learned-parameter categories (P5-learnable later). Triggers on the same semantic-memory opt-in as R35/R36/R37; default/recency/offline assemblies keep the constant feeling. Real interoceptive-signal integration and downstream feeling consumption are deferred. No new logging mechanism; full suite green and network-free |
| 39 | Memory-Grounded Uncertainty and Transport-Grounded Social Appraisal | draft | baseline_implementation | 03, 33, 34, 35 | P3 fifth cognitive-owner de-shim: two of `03`'s four remaining constant dimensions become real. `uncertainty` is grounded in retrieval ambiguity over the R34 embedding + R33 store substrate: a `RetrievalAmbiguitySource` returns the top-N cosine similarities, and `03` maps `uncertainty = clamp(1 - (n1 - n2))` over the normalized top-two (no comparable memory → `1.0`; one dominant match → low; several near-equal matches → high) — a distinct read of the retrieval result from novelty (which reads only the top match). `social` is grounded in transport provenance: a `SocialContextSource` returns a bounded `social_presence` fact (external interactive-agent channel like CLI operator → high; internal body/background → 0), and `03` maps `social = clamp(social_floor + social_gain * presence)`. Both salience mappings live in the new owner-owned `GroundedDimensionEstimator` (generalizing R35); composition supplies only raw facts and `03` imports neither the embedding, persistence, nor channel owners. The fast pre-attentive path stays deterministic, network-free, and LLM-free. Honest grounding: uncertainty is `B_functional_inspiration` (retrieval-ambiguity proxy, not calibrated confidence); social does not require the embedding substrate and is bundled under the semantic opt-in only for a single rollout switch (documented). `threat`/`reward` remain first-version constants pending R40 (network-free prototype-embedding, weaker `C_engineering_hypothesis` grounding). Triggers on the same semantic-memory opt-in as R35-R38; default/recency/offline keep constant uncertainty `0.3`/social `0.0`; novelty unchanged. No new logging mechanism; full suite green and network-free |
| 40 | Prototype-Grounded Threat and Reward Appraisal | draft | baseline_implementation | 03, 34, 39 | P3 sixth cognitive-owner de-shim: the last two constant `03` dimensions become real, so all five `03` dimensions are now real and the `04` reward→dopamine / threat→cortisol channels are driven by real signals on every channel. `threat`/`reward` are scored by the stimulus's maximum cosine similarity to owner-owned prototype phrase sets (`THREAT_PROTOTYPES`/`REWARD_PROTOTYPES`), embedded through the R34 substrate: `03` defines a `PrototypeSimilaritySource` protocol and maps `dimension = clamp(gain * max(0, max_cosine), 0, 1)` (positive correlation; `None`/empty content → `0.0`); composition's `EmbeddingPrototypeSimilaritySource` embeds the owner-provided phrases once (cached) and returns the raw cosine, so `03` imports neither the embedding nor persistence owner. The prototype sets (the definition of "what counts as threat/reward") and the mapping live in the `03` owner. Fast path stays deterministic, network-free, LLM-free; no cold-start (prototypes embedded at assembly time). **Honest grounding `C_engineering_hypothesis`:** the prototype phrase set is a hand-authored, English-centric PLACEHOLDER anchor, not a calibrated affective model; it is the surface a later slice replaces (P5 learning of prototypes/gains, `06` memory-affect grounding from past outcome valence, or a slow `11`-LLM second-stage re-appraisal). Recorded everywhere and not over-claimed. Triggers on the same semantic-memory opt-in as R35-R39; default/recency/offline keep constant threat `0.2`/reward `0.1`; novelty/uncertainty/social unchanged. No new logging mechanism; full suite green and network-free |
| 41 | Dimension-Grounded Aggregate Salience Judgment | draft | baseline_implementation | 03, 35, 39, 40 | P3 `03`-owner closeout: the sixth `03` output — the aggregate salience judgment (`RapidSalienceVector.aggregate`) — becomes real, so under the semantic-memory assembly every `03` output (five dimensions + aggregate) is a real signal, none constant. The constant `FirstVersionAggregateEstimator` (which ignored the dimensions and returned `0.4`) is replaced by an owner-owned `WeightedAggregateEstimator` computing `aggregate = clamp(sum(weight_k * dimension_k), 0, 1)`, a convex combination of the five real dimensions with explicit first-version weights summing to 1.0 (`threat 0.25, reward 0.25, novelty 0.20, uncertainty 0.15, social 0.15`). The combination is owned by `03`, monotonic non-decreasing in each dimension, deterministic, bounded, stateless; it needs no injected fact source (a pure function of the dimensions). Honest caveat: the weights are a first-version PLACEHOLDER allocation (an engineering choice, not a calibrated importance prior; P5-learnable), and the aggregate inherits its inputs' grounding strength — while threat/reward are the R40 `C_engineering_hypothesis` anchor, the aggregate's threat/reward contribution is only as strong as that anchor. Future: P5 weight learning, model-assisted/non-linear overall appraisal, affect/recency weighting. Reuses the same semantic-memory opt-in as R35-R40; default/recency/offline keep constant `0.4`; the five dimensions unchanged. No new logging mechanism; full suite green and network-free |
| 42 | Durable Runtime Continuity Checkpoint and Restart Resumption | draft | baseline_implementation | 33, 22, 09, 18 | P2 third slice (continuity checkpoint). Establishes the durable runtime-continuity checkpoint owner (`helios_v2.continuity_checkpoint`): a `RuntimeContinuitySnapshot` contract (an owner-neutral serializable projection of the genuinely cross-tick continuity state — the `09` `ContinuationPressureState` and the `18`/`24` long-horizon continuity, i.e. deferred-continuity records plus continuity threads — reusing those owners' own contracts verbatim), a `CheckpointStoreBackend` protocol with a first-version single-row SQLite file backend plus a deterministic in-memory double, and a `ContinuityCheckpointStore` facade (`save_latest` replace / `load_latest` or explicit absence). On an opt-in `assemble_runtime(continuity_checkpoint=...)` the runtime saves the latest snapshot after each tick (mirroring the `_persist_experience` carry seam, reading only owner-published stage-result values) and restores it at assembly/startup, seeding the `09` thought-gating and `18` autonomy stages' prior cross-tick state through explicit owner-neutral stage seed seams, so after a process restart against the same file the system resumes its prior continuation pressure and continuity threads instead of starting inert (advancing `FG-5.1`). Reconstruction runs the owners' own contract validation; a cold store keeps the existing inert defaults; a corrupt/invariant-violating snapshot fails fast on load; `continuity_checkpoint_ready` fails fast when the backend cannot initialize; no degraded non-persistent path once enabled. Independent of `33`/`34` (persists a different state). `04`/`05`/`14`/`06` state is out of scope (not cross-tick in-process today; the snapshot is versioned for additive extension). Opt-in and default-off: default, `33`, `34`, and channel-bound assemblies byte-for-byte unchanged; no new logging mechanism; checkpoint file git-ignored; full suite green and network-free |
| 43 | Dual-Timescale Neuromodulator Dynamics and Checkpoint | draft | baseline_implementation | 36, 04, 42 | P2/P3 hinge slice making `04` affect evolve across ticks. The `04` neuromodulator update becomes a deterministic dual-timescale leaky-integrator that carries the prior-tick levels forward: per channel `next = clamp(prior + alpha_phasic * (drive - prior) + alpha_tonic * (baseline - prior), legal_min, legal_max)`, where `drive` is the R36 instantaneous appraisal-derived target (`tonic_baseline + sum(sensitivity_k * salience_k)`), `alpha_phasic` is the fast stimulus-tracking rate and `alpha_tonic` the slow baseline-regression rate (`0 < alpha_tonic < alpha_phasic <= 1`, both under the already-declared `decay_speed_persistence` learned-parameter category, P5-learnable). The instantaneous drive stays owned by the injected R36 path; the new dual-timescale carry/decay is an owner-owned `DualTimescaleNeuromodulatorUpdatePath` in `helios_v2.neuromodulation` that wraps the inner drive path. The `NeuromodulatorUpdatePath` protocol and `NeuromodulatorSystemAPI.update_state` gain an additive optional `prior_levels`/`prior_state` (default `None` reproduces the stateless behavior byte-for-byte); the `NeuromodulatorRuntimeStage` holds the prior-tick state (like `09`/`18`), feeds it each tick, and exposes a `seed_prior_state` restore seam. Cold start (no prior / cold checkpoint) defaults prior to the tonic baseline (one integrator step from baseline; no fabricated history); the integrator is provably bounded (clamped, `alpha` in `(0,1]`); an unstable `alpha` ordering is rejected at construction. The R42 `RuntimeContinuitySnapshot` is bumped to `snapshot_version = 2` with an additive `neuromodulator_levels` field, so the cross-tick `04` state survives a restart through the checkpoint (save reads the published `04` levels; restore seeds the stage); a version-mismatched or corrupt-levels payload is rejected on load (no v1 migration). Activates under the same semantic-memory opt-in as R36; default, recency-only, and offline assemblies keep the stateless constant `04`. Deferred: cross-channel coupling, P5 coefficient learning, and the cortisol/inhibition hard gate. No new logging mechanism; full suite green and network-free |
| 44 | Dual-Timescale Interoceptive Feeling Persistence and Checkpoint | draft | baseline_implementation | 38, 05, 43 | P2/P3 slice making `05` felt body-state evolve across ticks — the `05` mirror of R43, completing the affect pair. The `05` interoceptive feeling construction becomes a deterministic dual-timescale leaky-integrator (same form as R43) that carries the prior-tick feeling forward: per dimension `next = clamp(prior + alpha_phasic * (target - prior) + alpha_tonic * (baseline - prior), legal_min, legal_max)`, where `target` is the R38 instantaneous neuromodulator-derived feeling, `alpha_phasic` the fast tracking rate and `alpha_tonic` the slow baseline-regression rate (`0 < alpha_tonic < alpha_phasic <= 1`, under the already-declared `feeling_persistence` learned-parameter category, P5-learnable). The instantaneous target stays owned by the injected R38 `NeuromodulatorDerivedFeelingConstructionPath`; the new persistence carry is an owner-owned `PersistentFeelingConstructionPath` in `helios_v2.feeling` that wraps it. The `FeelingConstructionPath` protocol and `InteroceptiveFeelingAPI.update_state` gain an additive optional `prior_feeling`/`prior_state` (default `None` reproduces the stateless behavior byte-for-byte); the `InteroceptiveFeelingRuntimeStage` holds the prior-tick state (like `04`/`09`/`18`), feeds it each tick, and exposes a `seed_prior_state` restore seam. Cold start defaults prior to the baseline feeling (one integrator step from baseline; no fabricated history); the integrator is bounded; an unstable `alpha` ordering is rejected at construction. The `RuntimeContinuitySnapshot` is bumped to `snapshot_version = 3` with an additive `feeling` field, so the cross-tick `05` feeling survives a restart through the R42 checkpoint (save reads the published feeling; restore seeds the stage); a version-mismatched (v1/v2) or corrupt-feeling payload is rejected on load (no migration). Activates under the same semantic-memory opt-in as R38/R43; default, recency-only, and offline assemblies keep the stateless constant `05`. Also removes a pre-existing dead duplicate `NeuromodulatorDerivedFeelingConstructionPath` definition (hygiene). Deferred: real interoceptive-signal integration, P5 coefficient learning, feeding the evolving `05` feeling into `06`/behavior (FG-2). No new logging mechanism; full suite green and network-free |
| 45 | Affect-Grounded Memory Formation and Durable Memory Store | draft | baseline_implementation | 06, 33, 34, 05 | P2 closeout / P3 mid-chain de-shim of `06`. Closes the two coupled `06` shims together. Formation: a `06`-owned `AffectGroundedMemoryFormationPath` forms affect-tagged memory from the real `05` `InteroceptiveFeelingState` (the formed item's `affect_tag` is the genuine felt body-state, not a constant), with an owner-owned episodic-vs-autobiographical family mapping (mismatch evidence → autobiographical). Salience gate: a `06`-owned `SalienceGatedReplayCandidateSelector` computes a bounded affect-intensity over the real feeling (arousal/tension/pain) and mismatch, setting each replay candidate's `forced_consolidation` + `priority_hint` from it under the declared `consolidation_policy`/`replay_priority_policy` learned-parameter categories, so a flat low-affect tick consolidates nothing and a high-affect or high-mismatch tick consolidates. Durability: `PersistedExperienceRecord` gains an additive `record_kind` discriminator (default `experience_writeback`, so `15` records stay byte-for-byte) plus an opaque `metadata` map; the SQLite backend adds the columns via a PRAGMA-guarded `ALTER TABLE` so an existing R33/R34 file upgrades in place (legacy NULL `record_kind` reads back as the default). An owner-neutral `MemoryRecordBridge` + a `RuntimeHandle._persist_memory` carry seam (mirroring `_persist_experience`) durably persists exactly the `forced_consolidation` items as `record_kind="affect_memory"`, embedded at write, co-residing with the `15` stream. Recall: the existing `SemanticStoreBackedDirectedMemoryCandidateProvider` ranks every embedded record, so affect-memory becomes recall-eligible through `10` on the shared surface and survives a restart; `_record_tier` is family-aware (by stored kind only). Opt-in on the existing semantic-memory opt-in (store + embedding both present); default and recency-only assemblies keep the constant `06` shim. `06` imports neither the persistence nor embedding owner; the carry seam re-derives no decision (filters by the flag `06` set). A composition error without store+embedding; an embedding/store failure is a hard stop; no dedup/merge in this slice (deferred under `consolidation_policy`). No new logging mechanism; full suite green and network-free (607 passed) |
| 46 | Workspace Competition De-Shim | draft | baseline_implementation | 07, 06, 05 | P3 mid-chain de-shim of `07` (the attention bottleneck), building on R45's real `priority_hint`. Competition: a `07`-owned `SalienceWeightedWorkspaceCompetitionPath` scores each workspace candidate as a bounded function of the candidate's real `06` `priority_hint` + the real `05` feeling salience (`score = clamp(0.6*priority + 0.4*feeling_salience)`), replacing the constant `workspace_score_hint=0.95`; every replay candidate stays in the published set (forced flag + feeling provenance preserved verbatim, so the owner's existing invariants hold). Attention bottleneck: a `07`-owned `BoundedAttentionRetentionPath` retains only the top-K scoring subset (first-version `max_retained=3`, under `working_state_update_policy`) into the working state with a deterministic candidate-id tie-break and a never-empty guarantee for a non-empty set, replacing the retain-everything shim. Brain-aligned semantic (owner-confirmed): "consolidated" (a `06`-forced candidate, persisted long-term) is distinct from "held in attention" (the bounded working state), so a forced candidate may lose the attention competition and not be held this tick — it stays in the candidate set (still reaching `08` as material) and is still persisted, only not in this tick's focus. Weights/bound are explicit first-version constants under the config's existing learned-parameter categories (P5-learnable). Opt-in on the same semantic-memory opt-in as R45 (the real `priority_hint` only exists once `06` is de-shimmed); default and non-semantic assemblies keep the constant-score / retain-everything `07` shim. No contract change (`WorkspaceCandidateSet`/`WorkingStateSnapshot` unchanged); `07` imports no other owner; all existing owner invariants still fail fast. No new logging mechanism; full suite green and network-free (618 passed) |
| 47 | Conscious Ignition Commitment | draft | baseline_implementation | 08, 07, 46 | P3 mid-chain de-shim of `08`'s commitment focal-selection, building on R46's real `workspace_score_hint`. Problem: the count-based first-version `_RetainedWorkingStateSelectionPolicy` declared `no_commit/semantic_conflict_unresolved` whenever more than one candidate was retained — and R46's bounded top-K working state retains >1 by design, so `08` would rarely become conscious of anything. Fix: an owner-owned `IgnitionFocalSelectionPolicy` (injected through the existing `focal_selection_policy` seam, living in `helios_v2.consciousness`) ignites the single highest-`workspace_score_hint` retained candidate as focal reportable content (global-workspace winner-take-all, deterministic tie-break by `source_workspace_candidate_id`) and demotes the rest to supporting context (ordered by descending score, bounded by the renderer's `max_supporting_context_items` cap). Preserved verbatim: `no_commit/insufficient_commitment_signal` (zero retained) and `no_commit/context_not_reportable` (empty focal summary); `semantic_conflict_unresolved` stays in the taxonomy for a future genuine-conflict slice but is no longer emitted for mere multiplicity. No contract change, no engine change, no renderer change (ignition produces exactly the focal+supporting shape the engine validation and the deterministic renderer already accept). Opt-in on the same semantic-memory opt-in as R45/R46; default and non-semantic assemblies keep the count-based policy. `08` imports no other owner; all existing owner invariants still fail fast. End-to-end the chain currently forms one candidate per tick, so the headline behavioral win (multiplicity → ignite winner instead of no-commit) is owner-level tested today and becomes end-to-end visible once a multi-candidate source lands. No new logging mechanism; full suite green and network-free (626 passed) |
| 48 | Workspace-Grounded Thought-Gate Activation | draft | baseline_implementation | 09, 07, 37, 46 | P3 de-shim of the `09` gate's `global_activation_level` (its second-largest non-stimulus term, weight `* 0.20`), building on R46's real `workspace_score_hint` and reusing the R37 gate-signal bridge seam. Under the semantic-memory assembly the gate-signal bridge (`NeuromodulatorAwareThoughtGateSignalBridge`) now sources `global_activation_level` from the same tick's `07` `WorkspaceCompetitionStageResult` — the maximum `workspace_score_hint` among the retained working-state candidates (the dominant ignition strength held in attention), or `0.0` when nothing is retained — instead of the constant `0.9`. This is the global workspace's real activation level, so the gate score now reflects how strongly the workspace actually ignited. Owner-neutral glue: the bridge forwards a raw bounded activation fact (clamped/rounded to `[0,1]`); the `09` owner keeps sole ownership of the gate decision and the `global_activation_level` weight (the gate path is unchanged). The R37 `neuromodulatory_arousal` coupling is preserved (both real facts ride the snapshot). `07` runs before `09`, so a missing/wrong-typed workspace result is a hard fail (the existing `RuntimeStageExecutionError`), never a silent fallback. No contract change to `ThoughtGateSignalSnapshot`/`ThoughtGateResult`; the real value surfaces in `contributing_signals["global_activation_level"]`. Opt-in on the same semantic-memory opt-in as R37/R46; default and non-semantic assemblies keep the constant `0.9`. The other four constant gate inputs (`workload_pressure`, `temporal_signal`, `drive_urgency_signal`, `dmn_available`) and the `selected_stimuli` projection remain first-version constants (no real producer running before `09` yet — `drive_urgency_signal` is owned by `18` which runs after `09`; the rest need unowned compute/clock/DMN producers). No new logging mechanism; full suite green and network-free (631 passed) |
| 49 | Thought-Directed Retrieval Recall Intent | draft | baseline_implementation | 10, 11, 09 | P3 de-shim of the `10` directed-retrieval request's `recall_intent`/`selected_memory_refs` (the query-planning path itself was already real; only its inputs were shim). The constant `recall_intent="remember runtime chain context"` and fabricated `selected_memory_refs=("memory:runtime:{tick}",)` are replaced, under the semantic-memory assembly, by the prior tick's `11` internal-thought `MemoryHandoffDirective` (`recall_intent` + `selected_memory_refs`, when `11` saved one for the next tick), so a line of thought the system chose to continue actually steers what memory it retrieves next tick — closing the memory-guided-maintenance loop (`ARCHITECTURE_PHILOSOPHY` §5.3). The carry mirrors the R32 consequence-claim / R42 continuity carry: an owner-neutral `PriorThoughtRecallHolder`, a post-tick `RuntimeHandle._carry_recall_directive` capture, and a `ThoughtDirectedRetrievalRequestBridge` that reads it. When there is no saved handoff (the first tick, a non-fired tick, or a tick where `11` did not continue) the request falls back to the real `09` `compact_stimuli` with no recall intent — a defined behavior, not a degraded path, and always valid since `compact_stimuli` is real. Owner-neutral: composition transports the `11`-owned directive verbatim and computes no retrieval policy; `10` and `11` contracts/engines are unchanged. Opt-in on the same semantic-memory opt-in as R45-R48; default and non-semantic assemblies keep the constant recall intent. No new logging mechanism; full suite green and network-free (635 passed) |
| 50 | Runtime Interoceptive Signal Source | draft | baseline_implementation | 02, 05 | Closes the producer half of `gap_interoceptive_signal_source`: establishes a new owner `helios_v2.interoception` that reports the runtime's real internal condition (compute/runtime pressure) as bounded interoceptive `RawSignal`s into sensory ingress, so the `02 -> 05` body-signal afferent path (which already existed in code but was always empty) finally carries real signals. Ships a `RuntimePressureSample` contract (four `[0,1]` channels: cpu/memory/latency/error), an injected `RuntimePressureSampler` protocol, a first-version `StdlibRuntimePressureSampler` (real CPU/memory via lazy `psutil` else stdlib load-average else a defined neutral default; latency/error are injectable first-version defaults; never raises for a merely-unavailable fact), and a `RuntimeInteroceptiveSource` implementing the existing `SensorySource` protocol (one bounded `signal_type="interoceptive"` `RawSignal` per channel, deterministic for a fixed sample). Sensory normalizes them to `modality="interoceptive"` stimuli that the `05` stage already filters and `validate_internal_body_signal` accepts, so the `05` feeling stage receives non-empty validated `internal_signals`. Scope (no half-step): R50 delivers the producer and the live, validated afferent; `05`'s construction path still ignores `internal_signals` this slice (its feeling value is unchanged), so the gap note distinguishes "producer exists" from "`05` consumes it" (the next slice). The owner holds no feeling/salience/cognitive policy and imports no feeling/appraisal/neuromodulation owner; an outright sampler exception propagates (no fabricated healthy body). Opt-in (`assemble_runtime(interoceptive_sampler=...)`); default/channel-bound/semantic assemblies byte-for-byte unchanged when off; no new mandatory/network dependency (psutil lazy + degrades). No new logging mechanism; full suite green and network-free (650 passed) |

| 51 | Interoceptive-Signal-Shaped Feeling | draft | baseline_implementation | 05, 50, 04 | P3 FG-2 closure: closes the consumption half of `gap_interoceptive_signal_source` and forms the first end-to-end FG-2 causal chain. R50 delivered the interoceptive afferent live into `05` (`internal_signals`), but `05`'s construction path ignored it (`del internal_signals`), so the felt body-state was still top-down from `04` alone. R51 adds a `05`-owned `InteroceptiveSignalModulatedFeelingConstructionPath` that wraps the R38 neuromodulator-derived target and adds a bounded, non-negative, stress-directional per-dimension contribution from the real compute/runtime-pressure afferent (cpu→arousal/tension, memory→fatigue/tension, latency→fatigue/tension, error→pain_like/tension), reading the bounded `pressure_channel`/`pressure_value` facts from the stimulus metadata the `50` producer set (not parsing content; max per channel; unrecognized/out-of-range/non-numeric facts contribute nothing and never raise). The contribution is additive over the inner neuromodulator target (never replaces it) and clamped to the legal range; empty/unrecognized `internal_signals` reproduces the inner target byte-for-byte. Composition nests it as `PersistentFeelingConstructionPath(InteroceptiveSignalModulatedFeelingConstructionPath(NeuromodulatorDerivedFeelingConstructionPath()))` only under the semantic-memory assembly with an interoceptive sampler present, so the body contribution flows through the same R44 dual-timescale carry (no second persistence mechanism). Because `05` feeling already feeds `07` workspace competition (R46 reads arousal/tension/pain as `feeling_salience`), a high-pressure sample now measurably changes both the `05` feeling-state and the `07` candidate score versus an at-rest sample — the real "machine condition → feeling → workspace competition" chain, reconstructable by evaluation. The mapping lives in `helios_v2.feeling`; `05` imports no interoception/appraisal/neuromodulation/workspace owner. valence/comfort/social_safety are untouched this slice (narrow, monotone first version); coefficients are first-version constants under `feeling_coupling_strength` (P5-learnable). Opt-in on the existing semantic + interoceptive assembly; default, recency-only, channel-bound-without-interoception, and semantic-without-sampler assemblies are byte-for-byte unchanged. No new logging mechanism; full suite green and network-free (664 passed) |

| 52 | Workspace Multiplicity from Recalled Affect-Memory Replay | draft | baseline_implementation | 06, 07, 33, 34, 45 | P3 multiplicity activation: the chain formed exactly one workspace candidate per tick (one binding context → one `06` memory item → one replay candidate), so R46 competition, R47 winner-take-all ignition, and R48 gate activation were real but never exercised over genuine multiplicity (R47's own note flagged this). R52 has the `06` owner surface recalled prior affect-memories as additional, non-`forced_consolidation` replay candidates alongside the current-tick memory, giving the `07` workspace a real multiplicity to arbitrate (hippocampal-style replay competing for the global workspace). A `06`-owned recalled-replay path re-forms each injected `RecalledMemoryFact` into an `AffectTaggedMemoryItem` preserving the recalled `memory_id`, stored `family`, and original persisted `affect_tag` (anchored to the current feeling state + binding context, so all existing `MemoryFormationState` invariants hold) plus a non-forced `MemoryReplayCandidate` whose `priority_hint` comes from an owner-owned bounded blend of recall relevance + recalled affect intensity (under `replay_priority_policy`, P5-learnable). The recalled-memory source is injected behind a narrow `RecalledMemoryProvider` protocol (the `06` owner imports neither persistence nor embedding); composition's `StoreBackedRecalledMemoryProvider` embeds the current binding-context content and ranks `affect_memory`-kind records by cosine similarity (reusing the R34 `search_similar`), reconstructing each recalled affect vector from the durable record. To enable faithful recall, the R45 `MemoryRecordBridge` now additionally writes the formed memory's affect vector into the affect-memory record's opaque `metadata` (an additive string-encoded extension; no persistence contract change; legacy records without it are simply not workspace-recall-eligible). End-to-end under the semantic assembly, once a prior consolidation-worthy affect-memory exists, a later tick's `07` competes over >1 candidate, `08` ignites the single highest-scoring retained candidate as focal content (rest demoted to supporting context), and `09` `global_activation_level` equals the max retained score — R46/R47/R48 now exercised over genuine multiplicity, and a sufficiently strong recalled memory can win the workspace. Recalled candidates are additive and never `forced_consolidation` (so the R45 persistence carry does not re-store them); the current-tick formed memory and its gate are unchanged. A cold store / empty binding context / no similar memory yields zero recalled candidates (single-candidate behavior unchanged); an embedding/store failure during recall is a hard stop (no silent single-candidate fallback). Opt-in on the semantic assembly; default, recency-only, channel-bound-without-semantic, and offline assemblies are byte-for-byte unchanged; existing store files read back. No new logging mechanism; full suite green and network-free (678 passed) |

| 53 | Workload Pressure from the Interoceptive Afferent | draft | baseline_implementation | 09, 50, 51 | P3 gate-input de-shim and the second interoceptive consumer (after `05` feeling, R51). The `09` gate's `workload_pressure` was hardcoded `0.1` in the composition gate-signal bridges, yet in the `09` owner it is a real, load-suppressive term (subtractive `* 0.45` in the gate score, and above `resource_pressure_block_threshold` with low continuation it blocks firing with `resource_pressure_too_high`) — so the most load-sensitive gate term was driven by a constant. R53 grounds it in the runtime's real compute load: an owner-neutral `_interoceptive_workload_pressure(frame)` helper reads the R50 interoceptive cpu/memory load stimuli already present in the same tick's `02` sensory batch (from the reserved `pressure_channel`/`pressure_value` metadata; max per channel = dominant resource pressure; unrecognized/out-of-range/non-numeric skipped, never raising) and both gate-signal bridges (`NeuromodulatorAwareThoughtGateSignalBridge` semantic, `FirstVersionThoughtGateSignalBridge` default) forward it in place of `0.1`. The `09` owner keeps sole ownership of the gate weight and block threshold; the bridge forwards a raw bounded fact only and imports no interoception owner (the afferent flows through `02`). Real machine load now monotonically raises the gate's resource-pressure term, surfacing in `contributing_signals["workload_pressure"]`. **Surfaced constraint (recorded honestly):** because high load correctly drives the gate to no-fire and the assembled chain has no gate-no-fire closure yet (directed retrieval raises on a non-fired gate), R53 exercises the real value end to end only within the firing window (cpu/memory up to ~0.3 given the other first-version constant gate signals) and validates the high-load → high-`workload_pressure` → block relationship at the owner-neutral helper level; the full-range end-to-end exercise is unlocked by a dedicated gate-no-fire tick-closure requirement (the gating-no-fire analog of R28's `internal_only` closure). Absence of a recognized load stimulus keeps the constant `0.1` byte-for-byte, so the default, recency-only, channel-bound-without-interoception, and semantic-without-sampler assemblies are unchanged. No new contract; no new logging mechanism; full suite green and network-free (685 passed) |

| 54 | Gate No-Fire Tick Closure | draft | baseline_implementation | 09, 28, 53, 13, 15, 18, 17 | P3 enabler surfaced by R53. The assembled `01-18` chain could not complete a tick whose `09` gate decided `no_fire`: the post-gate stages (`10` directed-retrieval, the `16` prompt/outward-expression/externalization family, `11` internal-thought, `12` action-externalization, `14` identity-governance) hard-require a fired `ThoughtGateResult` (deliberate owner invariant) and raised. This blocked R53 (a high real compute load correctly decides `no_fire` via `resource_pressure_too_high`, then crashed) and would block R55/R56. R54 closes the no-fire tick as the upstream mirror of R28's fired-but-no-proposal internal-only closure. Each post-gate stage result gains an additive `activated: bool = True` discriminator (plus `inactive_id` and Optional artifact fields, all defaulting to the fired shape) and an `inactive(tick_id)` factory; each post-gate stage `run` observes the gate owner's published `decision` (or the upstream result's `activated` flag) and returns an explicit not-activated result without invoking its owner's fired-path API, so those owners' "requires a fired gate" invariants are never violated and never relaxed. The closure tail reuses R28 entirely: the planner stage's no-fire branch synthesizes an owner-neutral no-fire marker externalization result (`status="no_externalization"`, no fabricated proposal) and routes it through the existing `evaluate_internal_only` → `no_actionable_proposal`; the writeback bridge already emits an `internal_only` continuity record for that outcome (with a governance-inactive guard added); the `18` autonomy and `17` evaluation owners still run (autonomy integrates continuation/continuity regardless of firing; evaluation is read-only) from no-fire-marker-anchored requests with no-action drive inputs and explicit `activated=False` evidence — so continuation pressure and `18`/`24` long-horizon continuity carry across a no-fire tick and the tick is diagnostically reconstructable rather than aborting. No fabricated cognition: inactive results carry `None` artifacts and the closure-tail markers are ids only. The fired path is byte-for-byte unchanged (every new field defaults to the fired shape; no-fire branches are reached only when `decision != "fire"`). This lifts the R53 firing-window constraint: a high-compute-load tick now completes end to end as a no-fire tick. Default (not opt-in) correctness fix; no new owner contract; no new logging mechanism; full suite green and network-free (690 passed) |

| 55 | Temporal Pacing and DMN Rest-State Gate Inputs | draft | baseline_implementation | 09, 02, 54 | P3 gate-input de-shim grounding the last two constant gate signals besides drive-urgency: `temporal_signal` (was constant `0.4`) and `dmn_available` (was constant `True`). Both are real, behaviorally-active gate terms (`temporal_signal * 0.10`, `+0.10 when dmn_available`) mapping to the default-mode-network rest-state dynamic the constants flattened. A new owner `helios_v2.temporal` provides a `TemporalSource` producing a bounded `TemporalPacingSample` (`temporal_signal` in `[0,1]`, `dmn_available` bool); the first-version `RestStateTemporalSource` maps rest (no external stimulus this tick) to `dmn_available=True` and an external task to `False` (the DMN engages at rest, is suppressed on external task), and accumulates `temporal_signal` across consecutive no-fire ticks (`clamp(per_tick_increment * ticks_since_last_fire, 0, max_signal)`), resetting on a fire — the spontaneous-thought pacing of elapsed rest. Both gate-signal bridges gain an optional `temporal_source` and forward its outputs in place of the constants via a shared `_temporal_inputs(frame, source)` helper (reading the raw external-stimulus fact from the `02` batch through `_external_stimulus_present`); the `09` owner keeps the gate weights and decision semantics. The cross-tick elapsed state is the owner's first-class concept, advanced post-tick from the published `09` gate decision through an owner-neutral `RuntimeHandle._carry_temporal` seam (fire resets, no-fire increments). R54 made this safe: a real temporal input can now legitimately fail to push the gate over threshold (a no-fire tick) without aborting, so the runtime can express rest-state spontaneous-thought pacing (more likely to think the longer at rest) and DMN task-disengagement. The temporal owner holds no salience/feeling/cognitive policy and imports no gate/appraisal/feeling/neuromodulation owner; composition forwards raw facts only. Opt-in (`assemble_runtime(temporal_source=...)`); deterministic for a fixed observation sequence; default, recency-only, semantic, channel-bound, and interoceptive assemblies keep `temporal_signal=0.4`/`dmn_available=True` byte-for-byte when no source is wired. No new contract; no new logging mechanism; full suite green and network-free (702 passed) |

| 56 | Owner Boundary Recovery of Appraisal-Derived Neuromodulation | draft | baseline_implementation | 04, 36, 43, 22 | Owner-boundary recovery (no runtime behavior change). The R36 appraisal-derived neuromodulator drive mapping (`AppraisalDerivedNeuromodulatorUpdatePath` plus its private `_aggregate_salience`/`_AggregatedSalience` helper) — the `04` owner's defining cognitive policy of which appraisal salience drives which neuromodulator channel and how strongly (`reward_to_dopamine`, `novelty_to_norepinephrine`, `threat_to_cortisol`, … with `level = clamp(tonic_baseline + sum(sensitivity_k * salience_k))`) — was defined inside the assembly-only composition glue (`composition/bridges.py`), violating `ARCHITECTURE_BOUNDARIES.md` §4.5 / `ARCHITECTURE_PHILOSOPHY` §3.2/§7.1 (orchestration must not own a downstream owner's semantic judgment), while its R43 dual-timescale decay wrapper already lived in the `04` owner. R56 relocates the drive policy into the `04` owner package `helios_v2.neuromodulation` (importable there, reusing the owner's existing `_clamp`); composition now only constructs/injects/wraps it via the unchanged `NeuromodulatorUpdatePath` protocol. A new repository guard (`tests/test_composition_owner_boundary_guard.py`) fails if a `<salience>_to_<channel>` sensitivity coefficient reappears under `helios_v2/composition` (with a positive-control assertion so it is not vacuous). Behavior-preserving: identical neuromodulator levels for every batch/config and every assembly (default, recency-only, semantic, channel-bound, interoceptive, temporal, checkpoint) — a pure relocation, not a policy change. Explicitly out of scope and kept as accepted owner-neutral glue: the constant first-version shim paths (`FirstVersionNeuromodulatorUpdatePath`, `FirstVersionActiveChannelReporter`) and the pure projection bridges (which forward a published owner field with no scoring weight). `test_neuromodulator_engine.py` now imports the path from the `04` owner. No contract change; no new logging mechanism; full suite green and network-free (704 passed = prior 702 + 2 guard tests) |

| 57 | Owner Boundary Recovery of the Cognition-Derived Autonomy Drive Inputs | draft | baseline_implementation | 18, 29, 24, 28, 54, 22 | Owner-boundary recovery (no runtime behavior change), the autonomy analog of R56 and deeper because it coupled to the consumer owner's threshold. The cognition-outcome→autonomy-drive-input mapping lived in the assembly-only `FirstVersionAutonomyRequestBridge` (`composition/bridges.py`): tuned pressure constants (`_ACTION_CONTINUATION_PRESSURE=0.9`, `_ACTION_TEMPORAL_PRESSURE=0.4`, `_ACTION_IDENTITY_PRESSURE=0.4`, `_CONTINUE_/_CONCLUDED_*`, `_*_IDENTITY_PRESSURE`), the planner executed/blocked classification (`_PLANNER_*_STATUSES`), the retrieval-pull `/4.0` normalization, and — per the bridge's own docstring — constants reverse-engineered so `outward_drive = 0.9+0.4+0.4 = 1.7 >= 1.6` reaches the `18` owner's action threshold. "How strong a proactive drive a cognition outcome produces relative to my own action threshold" is the `18` owner's defining judgment (violating `ARCHITECTURE_BOUNDARIES.md` §4.5 / `ARCHITECTURE_PHILOSOPHY` §7.1/§3.3). R57 adds an `18`-owned `ProactiveCognitionFacts` input contract (the raw facts composition may read: activated/has_action_proposal/continuation_requested/continuation_active/has_self_revision/planner_status/retrieval_hit_count) and an `18`-owned `AutonomyDriveInputProjection.derive_drive_inputs(facts)` that produces the five existing drive-input summaries, transcribing the mapping verbatim; the owner's `OUTWARD_ACTION_THRESHOLD = 1.6` becomes a named owner constant reused by `FirstVersionAutonomyPath`. The composition bridge is reduced to extracting raw facts, forwarding provenance ids, and calling the owner projection. `ProactiveDriveRequest` is unchanged in shape (existing autonomy/contract/stage-chain tests unaffected). The owner-boundary guard is extended to fail on an autonomy drive-pressure constant (`*_(CONTINUATION|TEMPORAL|IDENTITY)_PRESSURE = <num>`) or an `outward_drive >=` / `OUTWARD_ACTION_THRESHOLD =` reference under composition (positive-control asserted; scoped so it does not flag the legitimate `09` gate `workload_pressure` projection). Behavior-preserving: field-for-field identical `ProactiveDriveRequest` and byte-for-byte identical autonomy disposition for every fact combination (fired±action, continue, concluded, self-revision, each planner status, no-fire) and every assembly. No contract shape change; no new logging mechanism; full suite green and network-free (716 passed = prior 704 + 12 tests) |

| 58 | Runtime Profile Capability Bundle for the Composition Root | draft | baseline_implementation | 22 | Composition-root structural refactor (no runtime behavior change; not an owner-boundary fix — no cognitive policy involved). `assemble_runtime` had grown to nine loose capability seams (`recorder`, `gateway`, `deterministic_thought`, `channel_cli`+`cli_output_sink`, `experience_store`, `embedding_gateway`+`embedding_profile_name`, `continuity_checkpoint`, `interoceptive_sampler`, `temporal_source`) plus three dependency-surface overrides, with the cross-capability rule (`embedding requires store`) validated inline and the derived `semantic_memory_enabled` recomputed as a local and threaded through ~10 ternaries — each new capability meant threading another loose flag through a ~600-line function. R58 introduces a frozen, composition-owned `RuntimeProfile` capability bundle that groups these seams, owns the cross-capability validation in `__post_init__` (the `embedding requires store` `CompositionError`, relocated verbatim), and exposes the derived flag as the `semantic_memory_enabled` property (computed once). `assemble_runtime` gains an additive `profile=` parameter and a `_resolve_profile` helper: it still accepts every existing loose kwarg (built into a profile via an `_UNSET` sentinel that distinguishes omitted from explicit-None), and rejects combining an explicit profile with overlapping loose kwargs (`CompositionError`). The body rebinds its locals from the resolved profile, so all wiring is transcribed unchanged. `RuntimeProfile()` with all defaults is the current default assembly. Behavior-preserving: identical stage order, dependency specs, wired bridges/paths, and per-tick output for every assembly (default, deterministic, channel-CLI, persistence, semantic, checkpoint, interoceptive, temporal) and every existing caller (loose-kwarg path unchanged). The owner-boundary guard stays green (`RuntimeProfile` holds no cognitive policy). No new logging mechanism; full suite green and network-free (721 passed = prior 716 + 5 tests). Progress-flow maps unchanged (no owner maturity/stage-chain/boundary change) |

| 59 | Injectable External Afferent Source | draft | baseline_implementation | 02, 58, 31 | P3 FG-1 honesty slice: makes the external afferent a first-class injectable capability and stops counting the fabricated constant as a real signal. The `02 -> 03` chain appraises the whole batch, so a varying external stimulus genuinely drives `03 -> 04 -> 05 -> 07`; but the default/semantic assemblies registered a constant `FirstVersionSensorySource` (fixed `content="hello runtime"` every tick), so the external afferent was a composition-injected constant masquerading as input (violating `FG-1`), and because the content never changed the real `03` novelty collapsed to a fixed value after the first store write. R59 adds an optional `RuntimeProfile.external_signal_source` (conforming to the existing `02` `SensorySource` protocol); when provided, composition registers it as the external source in place of the placeholder, and the injected/`channel_cli` external-afferent positions are mutually exclusive (both-set is a `CompositionError`, validated in `RuntimeProfile.__post_init__`). A first-version `SequenceExternalSignalSource` replays caller-supplied, tick-varying real `RawSignal` batches and emits an explicitly empty batch when exhausted — it fabricates no content (no invented/randomized/cycled text), honoring `ARCHITECTURE_PHILOSOPHY` §4.3/§8 (no prompt theater); honest absence is a defined behavior (the tick closes through the existing no-fire/internal-only path). A semantic-assembly test proves a varying external stimulus produces measurably different `03` novelty and `04` state across ticks — a second `FG-2` causal chain (external afferent) alongside R51's interoceptive one. The default placeholder is now documented as NON-REAL; R59 does not make the default real (that needs a real deployed source, network `wave_C`), it makes a real source injectable and retires the constant-as-real-afferent. Opt-in and default-off: default/recency/semantic/checkpoint/interoceptive/temporal assemblies byte-for-byte unchanged when off. No contract change; no new logging mechanism; full suite green and network-free (728 passed = prior 721 + 7 tests) |

| 60 | Memory Binding Context Derived from the Real Perceived Stimulus | draft | baseline_implementation | 06, 45, 52, 59, 02 | P3 FG-1 memory-content honesty slice. The `06` memory owner forms an affect-tagged memory each tick; since R45 the affect tag is the real `05` feeling, but the memory's **content** came from `FirstVersionMemoryBindingContextBridge`, which returned a hardcoded constant in every assembly (`content_kind="situational-summary"`, `salient_tokens=("hello","novelty")`, `summary:runtime` refs). `AffectGroundedMemoryFormationPath` copies that content verbatim, so the durably-persisted (R45) and recalled (R52) memory was *about* a fixed constant unrelated to what the system actually perceived — a composition-injected constant violating FG-1, the next fabricated position after R59 made a real varying stimulus reach `02`/`03`. R60 rewrites the bridge to derive the binding-context content from the real `02` percept already in the frame: it prefers the external stimulus (falls back to the whole batch for interoceptive-only ticks), projects the primary stimulus into the `06` contract (`content_kind="perceived-stimulus-summary"`, `summary_ref`=real stimulus id, `context_ref`=real batch id, `salient_tokens`=an owner-neutral mechanical tokenization of the real perceived content — every token a substring of the percept, capped at 8, never invented). **Surfaced constraint (recorded honestly):** the pre-gate `02-08` chain requires a memory every tick (the `07` workspace owner raises on zero replay candidates; R54's no-fire closure only covers post-gate stages), so returning `None` on an empty percept would break the chain. On a completely empty percept (no external and no interoceptive stimulus — R59 empty source, channel no-input) the bridge therefore binds an honest no-percept marker anchored to the real `05` feeling state (`content_kind="no-perceived-stimulus"`, empty tokens, `summary_ref`=real feeling-state id) — no fabricated external content, no `None`. A genuine zero-percept pre-gate closure is a separate future requirement. Default-on correctness/honesty change (not opt-in); the default assembly's memory content now derives from the `FirstVersionSensorySource` placeholder percept ("hello runtime" → tokens `("hello","runtime")`) rather than a separate `("hello","novelty")` constant. The `06` affect tag, salience gate, durability, and recalled replay are unchanged in mechanism. No contract change; no new logging mechanism; full suite green and network-free (732 passed = prior 728 + 4 tests) |

| 61 | Prediction-Mismatch Evidence Grounded in Real Appraisal Novelty | draft | baseline_implementation | 06, 45, 03, 35, 59 | P3 FG-1 surprise/mismatch honesty slice. The `06` memory owner consumes optional `PredictionMismatchEvidence` for two decisions: the `SalienceGatedReplayCandidateSelector` folds `mismatch_score` into the consolidation salience, and `AffectGroundedMemoryFormationPath` maps a tick carrying mismatch to the `autobiographical` family (else `episodic`). Today `FirstVersionPredictionMismatchEvidenceBridge` returned a hardcoded constant (`mismatch_score=0.8/anomaly=0.85/confidence=0.9`) in every assembly, so surprise was asserted (every tick high mismatch → every memory autobiographical, consolidation floor always raised), not measured — the next fabricated position after R59/R60 made the percept and memory content real. R61 grounds the mismatch in the real `03` appraisal novelty already in the frame: the `03` `novelty` dimension is `1 - max cosine similarity` to stored experience ("unlike anything remembered"), the functional core of surprise in a memory-grounded system. The bridge reads the batch-max real `novelty`/`uncertainty` and projects `mismatch_score=clamp(novelty)`, `anomaly_score=clamp(novelty)`, `confidence=clamp(1-uncertainty)`; below `_MISMATCH_NOVELTY_THRESHOLD` (0.5, a composition-glue projection cut-point) a familiar/expected percept yields `None` (no surprise → the `06` owner forms an `episodic` memory). It computes no `06` gate/family policy and invents no forward-model prediction. **Honest grounding (`B_functional_inspiration`): this is novelty-as-surprise, NOT a true predictive-coding forward-model error (a later P5 concern); recorded and not over-claimed.** Default-on correctness/honesty change (not opt-in): in default/recency assemblies `03` novelty is the first-version constant `0.6` (≥ threshold) so they emit a `0.6`-derived mismatch (autobiographical) rather than the retired `0.8` constant; in the semantic assembly a cold/dissimilar percept → high novelty → high mismatch → autobiographical, and a percept similar to stored experience → low novelty → `None` → episodic. The `06` salience gate, family mapping, durability, and recalled replay are unchanged in mechanism. No contract change; no new logging mechanism; full suite green and network-free (735 passed = prior 732 + 3 tests) |

| 62 | Thought-Gate Drive-Urgency from the Prior-Tick Autonomy Drive | draft | baseline_implementation | 09, 18, 48, 55 | P3 `09` gate-input de-shim. **Scope converged during implementation** (recorded in the package): R62 originally bundled `drive_urgency_signal` + `selected_stimuli`, but projecting the real `03` appraisal for `selected_stimuli` drops the default (non-semantic) assembly's stimulus signal from the constant `0.9` to the real first-version aggregate `0.4`, pushing the default gate below the `0.55` fire threshold and flipping the default runtime to no-fire (17 fired-path tests depend on the default firing). That flip is honest (a weak constant-`0.4` appraisal should not fire every tick) but exposes a deeper "default-assembly has no real high-salience ignition source" problem that deserves its own requirement rather than being forced through by projecting a weak constant or patching the `09` owner threshold — so `selected_stimuli` is **deferred to R63** and R62 converges to `drive_urgency_signal` only. The `09` gate's `drive_urgency_signal` (weight `* 0.10`) was the constant `0.7`; it is meant to be the proactive drive's urgency, owned by `18` autonomy which runs *after* `09`, so the gate could only see a constant. R62 carries the prior tick's real `18` `ProactiveDriveState.outward_drive` (clamped to `[0,1]`) forward through an owner-neutral `PriorDriveUrgencyHolder` updated post-tick (the R49/R55 carry pattern) and read by the gate-signal bridge next tick; the `09` owner keeps the weight. The first tick uses a documented neutral cold-start equal to the prior constant (`0.7`), so tick 1 of every assembly is byte-for-byte unchanged and the real prior drive supersedes it only from tick 2 (e.g. an externalizing prior tick with `outward_drive ≥ 1.6` makes the next tick's gate `drive_urgency_signal = 1.0`). No fabricated urgency; absent `18` result leaves the carry unchanged. No contract change; no new logging mechanism; full suite green and network-free (738 passed = prior 735 + 3 tests). The gate's last remaining constant input is `selected_stimuli` (R63) |

| 63 | Real Selected-Stimuli Projection and Default-Assembly Ignition Source | draft | baseline_implementation | 09, 03, 41, 61 | P3 `09` gate-input de-shim closeout: the last constant shim in the gate signal. Both gate-signal bridges hardcode `selected_stimuli` (`stimulus_intensity=0.9`, `novelty_signal=0.6`, `sensitization_signal=0.2`), yet the real `03` appraisal result is already in the frame (the R61 mismatch bridge reads the same path). R63 projects the batch-max `03` `aggregate`→`stimulus_intensity`, `novelty`→`novelty_signal`, `uncertainty`→`sensitization_signal` (clamped, rounded) through an owner-neutral `_selected_stimuli_from_appraisal(frame, tick_id)` helper. Projecting the real `03` aggregate exposes a deeper problem: the default assembly's `FirstVersionAggregateEstimator` returns `0.4`, which drops the gate below the `0.55` fire threshold. R63 raises it to `0.7` — an honest moderate baseline ("a first-version system attributes moderate significance to its percept") that provides just enough ignition alongside the other real signals (gate score `~0.555 > 0.55`), while appraisal alone cannot force a fire (`0.7 * 0.30 = 0.21 < 0.55`). The semantic assembly uses `WeightedAggregateEstimator` (R41) and is unaffected. Absent `03` result falls back to documented cold-start constants. After R63 every `09` gate input is real; no constant shim remains in the gate signal. No `09` contract/threshold/weight change; no new logging mechanism |

| 64 | P3 Exit Evaluation | draft | baseline_implementation | 01-63 | P3 exit signal formal assessment (ARCHITECTURE_PHILOSOPHY §13.1): "emotional states genuinely evolve and traceably alter downstream decisions." Automated evaluation tests (`tests/test_p3_exit_evaluation.py`) validate: (FR1) FG-1 de-shim coverage — stages 03-10 all consume real non-constant signals under the semantic assembly; (FR2) FG-2.1 emotion evolves cross-tick — varying external stimuli produce measurably different 03/04/05 states; (FR3) FG-2.2 causal chains — both external (varying stimulus → 03 → 04 → 05 → 09) and internal (machine pressure → 05 → 07 → 09, R51) chains are traceable end-to-end; (FR4) structured pass/fail `P3ExitVerdict` report; (FR5) honest record of out-of-scope remaining shims (zero-percept gate, 13 planner bridge, 14 identity governance). Read-only evaluation layer; no owner code modified. Full suite green and network-free (746 passed) |

| 65 | Zero-Percept Pre-Gate Closure | draft | baseline_implementation | 06, 07, 08, 09, 54, 60, 59, 02 | P3 pre-gate mirror of R54's post-gate no-fire closure. The pre-gate `06→07→08` chain was forced to produce at least one memory and one workspace candidate every tick, because `07` workspace's `validate_memory_replay_candidates` raises on zero candidates. R60 addressed the memory-content-constant problem with an honest no-percept marker, but the chain still formed a memory. R65 lets a genuinely zero-percept tick (empty `02` batch — no external and no interoceptive stimulus) legitimately skip `06` memory formation, `07` workspace competition, and `08` consciousness ignition, closing through `09` gate's no-fire decision. Each pre-gate stage result gains an additive `activated: bool = True` discriminator plus `inactive_id` and an `inactive(tick_id)` factory (mirroring R54's post-gate pattern); each pre-gate runtime adapter checks the upstream result's `activated` flag (or, for `06`, the `02` batch's emptiness) and short-circuits without invoking its owner engine. The `09` gate receives a zero-signal snapshot (`global_activation_level=0.0`, empty `selected_stimuli`) when consciousness is inactive, and the engine's `commit_status != "committed"` check naturally produces `no_fire` with reason `conscious_content_not_eligible`. No owner code change; no contract change; no engine change. The R60 no-percept marker path in the binding-context bridge is annotated as a defensive fallback unreachable from the standard runtime path after R65. Default-on correctness fix (never reached in default/semantic/interoceptive/channel-bound/temporal assemblies, each of which registers at least one stimulus source). No new logging mechanism; full suite green and network-free (744 passed = prior 738 + 6 tests) |
| 66 | Fired Thought Non-Success Closure | draft | baseline_implementation | 27, 28, 54 | Closes the real-LLM-exposed runtime gap where an activated `11` thought result can legitimately be non-`completed` (`insufficient_generation` from malformed structured output) but the assembled runtime still assumed `completed` at `12` action externalization and `14` identity governance. Runtime orchestration now short-circuits that path without weakening owner contracts: `12` emits a provenance-preserving `no_externalization` marker result, `14` emits an explicit inactive marker, and the existing `13 -> 15 -> 18 -> 17` internal-only tail completes the tick as `no_actionable_proposal` / `written_internal_only` / `internal_only_decision`. No new owner contract, no fallback parsing path, and the completed-thought path remains unchanged |
| 67 | Stable Continuity Thread Key | draft | baseline_implementation | 18, 24 | Wave-B enabler: sharpens the `18` autonomy owner's continuity-key derivation to produce a tick-stable key derived solely from the `carry_reason` (via the existing `_base_reason` stripping helper), replacing the tick-specific key that embedded `source_thought_cycle_result_id` / `source_planner_bridge_result_id`. With the old scheme, thread reinforcement depended on an unbroken carry-forward chain (the carried record preserving the old key); when a record expired (2-tick `expires_after_ticks`) and a later tick created a fresh deferral for the same motive, the new key differed and the thread reset to age 1 / reinforcement_count 0. R67 makes the same deferral motive always map to the same thread regardless of gaps: `_continuity_key(carry_reason) -> base_reason(carry_reason)` (e.g., `"insufficient_outward_readiness"` or `"blocked_outward_externalization"`). `origin_ref` remains on `DeferredContinuityRecord` for tick-specific provenance. No contract shape change; no new logging mechanism; default-on (the key scheme is an internal `18` owner detail). Integration tests verify 5-tick consecutive reinforcement (reinforcement_count >= 2) and post-expiry gap reinforcement (fresh record matches prior thread). Full suite green and network-free (755 passed = prior 753 + 2 tests) |
| 68 | Identity Cross-Tick Governance Carry State | draft | baseline_implementation | 14, 18 | Wave-B `14` identity governance cross-tick carry: introduces a `GovernanceCarryState` frozen dataclass in the `14` owner's contracts (identity_state_snapshot, recent_governance_trace_history, accepted/rejected_revision_count with `__post_init__` validation). `IdentityGovernanceRuntimeStage` holds `_prior_carry_state: GovernanceCarryState | None` and advances it post-tick from the governance result (carrying the applied identity snapshot when a revision is accepted, preserving the prior snapshot otherwise; appending a bounded trace entry per tick; accumulating accepted/rejected counts). `FirstVersionIdentityGovernanceRequestBridge` gains an optional `carry_state_provider: Callable[[], GovernanceCarryState | None]`; when wired, `build_request()` injects the carry state's snapshot and trace history into the request (deriving a `governance_trace_summary` dict from the history). When no provider is set or it returns `None`, the bridge falls back to the bootstrap constant (byte-for-byte identical cold start). Assembly wires the provider as a closure over the stage's `prior_carry_state` property (owner-neutral: composition reads the published `GovernanceCarryState` type without interpreting it). `FirstVersionIdentityGovernancePath` is unchanged (already reads snapshot and trace from the request). No contract shape change to existing request/result; no engine modification; no new logging mechanism; default-on. Tests verify: cold-start byte identity, carry-state injection, trace accumulation across 5 ticks, second tick reads first tick's trace. Full suite green and network-free (762 passed = prior 755 + 7 tests) |
| 69 | Semantic Assembly as Default Runtime | draft | baseline_implementation | 22, 33, 34, 58 | Makes the semantic-memory assembly (the de-shimmed `03`–`10` cognitive chain) the default behavior of `assemble_runtime()`. Ships a `DeterministicHashEmbeddingProvider` in `helios_v2.embedding` (frozen dataclass, 16-dimension character-hash-to-bucket, no network, no model, no randomness — promoted from the test-only `_FakeEmbeddingProvider` pattern). Adds `RuntimeProfile.default_signal_mode` (`"semantic"` new default vs `"legacy_constant"` escape hatch, validated in `__post_init__`). When `default_signal_mode == "semantic"` and the caller did not inject `experience_store`/`embedding_gateway`, auto-provisions `InMemoryExperienceStoreBackend` + `DeterministicHashEmbeddingProvider` and rebuilds the frozen profile so `semantic_memory_enabled` reflects the provisioned state. Explicit caller-provided capabilities always take precedence. `assemble_runtime()` with no arguments now produces `semantic_memory_enabled == True`; real `03` novelty (not the constant `0.6`); real `04`–`10` chain. `assemble_runtime(default_signal_mode="legacy_constant")` preserves byte-for-byte pre-R69 behavior. Existing tests migrated via `default_signal_mode="legacy_constant"` in test helpers. No owner code change; no new bridge; no contract shape change. Full suite green and network-free (775 passed = prior 762 + 13 tests) |
| 70 | Prompt-to-Thought Real-State Bridge | draft | baseline_implementation | 16, 11, 02-10, 22 | Under the semantic-memory assembly, replaces the two constant-shim bridges (`FirstVersionEmbodiedPromptRequestBridge` and `FirstVersionInternalThoughtRequestBridge`) that feed the LLM thought loop and prompt-contract owner with semantic variants that read real `02`–`10` owner state from the tick frame and project it into bounded English text. `SemanticEmbodiedPromptRequestBridge` derives `stimulus_summary`, `state_summary`, and `retrieval_summary` from the real `02`/`05`/`09`/`10` stage results. `SemanticInternalThoughtRequestBridge` derives `internal_state_summary` from the real `03`/`04`/`05` stage results. `FirstVersion*Bridge` remains available under `legacy_constant` mode byte-for-byte unchanged. Assembly wiring follows the established ternary pattern. No contract shape change; only summary content changes under semantic assembly. Owner-neutral: bridges forward bounded raw facts, derive no cognitive policy. Closes the last major shim gap: the LLM thought loop now receives the brain's real affective/salience/feeling state. Full suite green and network-free (775 passed) |
| 71 | Performance Benchmark | draft | baseline_implementation | 01-70 | Automated performance metrics validation for P1/P2 exit targets defined in PHASE_METRICS.md. Read-only test suite (`tests/test_performance_benchmark.py`, 7 tests) measures: P1-P1 single-tick latency < 50 ms (legacy + semantic assembly), P1-P3 memory footprint < 500 MB over 100 ticks (tracemalloc), P2-P1 SQLite append throughput ≥ 100 records/s, P2-P2 semantic recall latency < 100 ms (1000 records), P2-P3 checkpoint save/load < 10 ms/tick. Composite verdict aggregates all benchmarks. No owner code modified; offline and network-free |
| 72 | P1 Exit Evaluation | draft | baseline_implementation | 01-70 | Automated P1 phase exit assessment per PHASE_METRICS.md §3 indicators. Read-only evaluation suite (`tests/test_p1_exit_evaluation.py`, 8 tests) validates: P1-T2 wave_A corroboration (17 produces corroborated/discrepant/unverifiable), P1-T3 wave_B continuity threads (18/24 persist ≥ 5 ticks), P1-T4 wave_C CLI round-trip (≥ 3 ticks), P1-T5 internal-only tick closure, P1-T6 no-fire tick closure (R54), P1-H1 read-only causal chain reconstruction (17/23), P1-H2 continuous 10-tick operation. Structured P1ExitVerdict report. No owner code modified; offline and network-free |
| 73 | P2 Exit Evaluation | draft | baseline_implementation | 01-70, 33, 34, 42-44 | Automated P2 phase exit assessment per PHASE_METRICS.md §4 indicators. Read-only evaluation suite (`tests/test_p2_exit_evaluation.py`, 10 tests) validates: P2-T2 store persistence (100 ticks), P2-T3 cross-restart continuity, P2-T4 semantic recall cosine ranking, P2-T5 checkpoint save/restore (v3), P2-T6 dual-timescale evolution (04/05), P2-H1 subjective continuity across restart, P2-H2 retrieval driven by real representation, P2-H3 embedding failure hard-stop, P2-H4 persistence paths defined. Structured P2ExitVerdict report. No owner code modified; offline and network-free |
| 74 | Owner Implementation Audit | draft | baseline_implementation | 01-18, 56, 57, 69, 70 | Read-only architectural governance audit (`tests/test_owner_implementation_audit.py`, 14 tests) across four dimensions: A1 contract completeness (every owner has contracts + init exports + support packages), A2 owner boundary compliance (3 regex guards on composition/bridges.py: no neuromodulator sensitivity policy, no autonomy drive pressure, no feeling coupling coefficients), A3 semantic assembly correctness (de-shim chain + novelty not constant), A4 R70 bridge integrity (semantic/legacy bridge wiring). Fail-fast validation (embedding requires store). Structured AuditVerdict report. No owner code modified; offline and network-free |
| 75 | Feedback Path Verification | draft | baseline_implementation | 04-10, 15, 18, 54, 62, 42 | End-to-end cognitive chain feedback path verification (`tests/test_feedback_path_verification.py`, 6 tests). FP-1: fire path → 18 autonomy carry (3 ticks). FP-2: no-fire closure → 18/17 still run (high load via pressure sampler). FP-3: writeback → memory → retrieval (5 ticks, store count ≥ 5). FP-4: 04→05→07→09 causal chain (dopamine/valence evolve). FP-5: checkpoint v3 save/restore round-trip. Structured FeedbackVerdict report. No owner code modified; offline and network-free |
| 76 | Memory Stability Assessment | draft | baseline_implementation | 33, 34, 42, 45 | Memory system stability assessment (`tests/test_memory_stability_assessment.py`, 7 tests) across six dimensions: MS-1 SQLite write durability (50 ticks ≥ 50 records), MS-2 semantic recall consistency (same query → same ranking), MS-3 cross-restart record integrity, MS-4 checkpoint v3 round-trip, MS-5 consolidation gating (affect-intensity threshold via SalienceGatedReplayCandidateSelector), MS-6 record kind coexistence (experience_writeback + affect_memory). Structured MemoryVerdict report. No owner code modified; offline and network-free |
| 77 | Long-Term Stability Prerequisites | draft | baseline_implementation | 01-70, 33, 42, 54, 56, 65 | Long-term operational readiness assessment (`tests/test_long_term_stability_prerequisites.py`, 7 tests) across six properties: LT-1 resource boundedness (20 ticks < 500 MB), LT-2 state isolation (2 independent handles, no cross-leakage), LT-3 checkpoint corruption recovery (corrupted file → fail-fast), LT-4 embedding failure isolation (raising provider → hard stop), LT-5 zero-percept + high-load closure (R65/R54 paths), LT-6 owner boundary non-regression (composition guard). Structured StabilityVerdict report. No owner code modified; offline and network-free |
| 78 | R70 Real-State Bridge Stage-Key Alignment | implemented | baseline_implementation | 70, 04, 05 | Corrects the stage-result key lookups in R70's `SemanticEmbodiedPromptRequestBridge` and `SemanticInternalThoughtRequestBridge` (`composition/bridges.py` L1980/L2085/L2097) to match the actual stage names published by `runtime/stages.py` (`interoceptive_feeling_layer` at L1105, `neuromodulator_system` at L1044). Before R78, all three lookups fell through the `isinstance` guard and silently returned the literal fallback strings ("neuromodulators at tonic baseline", "feeling at baseline"), fabricating an "at baseline" affective state to the LLM in every default-assembly tick. After R78, the 04 `NeuromodulatorLevels` (DA/NE/5-HT/ACh/Cort) and 05 `InteroceptiveFeelingVector` (arousal/valence/tension) reach the LLM user message as bounded English projections. Verified end-to-end via 4 new tests in `tests/test_r70_real_state_bridge_key_alignment.py`. No cognitive-owner code modified; bridges remain owner-neutral. P5-readiness enabler: the LLM now feels the real 04/05 trajectory, so P5 parameter changes will be reflected in the LLM context. Baseline 834 passed -> 838 total / 836 passed / 2 pre-existing R71 perf-flake failures (confirmed by stash-and-rerun on pre-R78 commit 0f34c2d) |
| 79 | Aggressive-Radical Prompt and Runtime Self-Talk | draft | baseline_implementation | 70, 16, 22, 25, 30, 04, 02, 03, 09, 42, 17 | Re-architects the v1 anti-theatrical structured-thought prompt path into a 7-sub-task stack (R79-A through R82) that lets the LLM be a person, the channel driver be the world, and the hormone system be the body. **R79-A delivered** (2026-06-11, commit `5fcc80a`): `R79AggressiveEmbodiedPromptPath` (sibling to `FirstVersionEmbodiedPromptPath`) implements the existing `EmbodiedPromptPath` Protocol with a 6-layer v3 contract (`present_field` / `embodied_state` / `attention_breakdown` / `channel_catalog` / `response_schema` / `v3_system_prompt`), 11-field natural-language JSON schema (`what_i_feel` / `what_i_think` / `i_want_to_say` / `i_will_send_it` / `i_send_through` / `i_want_to_act` / `act_type` / `remember_this` / `remember_because` / `i_want_to_think_more` / `think_more_about`), and 7 hard-rule cross-field invariants. Identity block tells the LLM "You are a person. Not an AI, not a role, not a runtime." Fail-fast on `prompt_bootstrap_id != "R79-aggressive-radical-v3"`. 11 unit tests in `tests/test_r79a_prompt_contract.py`. **R79-D delivered** (2026-06-11, commits `3827632` + `9597046`): extendable baseline framework at `tests/r79d/` (`framework.py` / `assertions.py` / `cli.py` / `scenarios/` / `reports/`) with 9 built-in assertions + `@register_assertion` seam, `_io` wrapper to satisfy R21 ad-hoc logging guard, 4 v1 baseline scenarios (A=continuous praise, B=continuous neglect, C=alternating bipolar, D=repeated stimulus 20 ticks); 52-tick real-LLM baseline run produces `aggregate.md` with 16/28 assertions PASS. **R79-B / R79-C / R80 / R81 / R82 pending** (same requirement package, separate sub-tasks). R79-B: channel catalog runtime injection + LLM channel arbitration. R79-C: 5-HT / Oxy / Opioid updater entries + `hormone_response_i_predict` LLM signal + `HormonePredictCorroborator`. R80: `internal_monologue` source owner for rumination feedback. R81: `_carry_internal_monologue` carry + `42` checkpoint v4 + `09` self-continuation gate signal. R82: 17-dim `BehaviorDriftDimension` + `R79DriftEvaluator` as P5 launch gate. Default assembly unchanged: v1 path is byte-for-byte unmodified; v3 is opt-in via `R79PromptProfile(prompt_path_mode="r79_aggressive_radical_v3")`. Baseline 831 passed -> 842 passed (831 + 11 R79-A), 0 regression; R21 ad-hoc logging guard green; composition owner-boundary guard green |

## Maturity Scale

The `Maturity` column is implementation-facing and must be reassessed whenever requirement, design, task, or code changes materially alter runtime coverage.

1. `not_started`
	- Requirement/design/task may exist, but no owner implementation has landed for the runtime concept.
	- Documentation-only progress does not count as implementation progress.
2. `pure_skeleton`
	- Code package or owner shell exists, but behavior is still mostly structural.
	- Typical signs: placeholder policies, pass-through wiring, missing runtime chain integration, or tests that validate only shapes rather than owner behavior.
3. `baseline_implementation`
	- The owner executes a real first-version behavior with fail-fast contracts and focused tests.
	- The core runtime path works end-to-end for the requirement's narrow owner responsibility, but major policy depth or downstream closure remains intentionally incomplete.
4. `relatively_complete`
	- The owner has a concrete first-version implementation, is integrated into its expected runtime path where applicable, and covers most of the requirement's intended owner behavior with focused and adjacent validation.
	- Further iteration may still deepen policy quality or later-stage integration, but the requirement is no longer mainly a scaffold or thin baseline.

## Authoring Rules

All requirement packages in Helios v2 must follow the same structure enforced by `d:/Software/project/helios/helios_v2/docs/requirements/requirement-authoring-standard.md`.

Additional Helios v2 constraints:

1. No requirement may specify fallback runtime behavior for critical capability loss.
2. Failure modes must be written as hard-stop or abort semantics.
3. Any adaptive policy must identify how it is learned, provided, or initialized without hardcoded runtime strategy branches.
4. Every design must name the module owner, the public API or ops it exposes, and the responsibilities it explicitly does not own.
5. Every task plan must show requirement -> design -> implementation order and must not skip directly to coding.
6. Every cross-module interface introduced by a requirement must be documented with comments or docstrings in code.
7. Every new public interface must follow `helios_v2/docs/API_AND_OPS_CONTRACT_GUIDE.md`.
8. Every material change to implementation coverage must update the `Maturity` column using the standard defined in `d:/Software/project/helios/helios_v2/docs/requirements/requirement-authoring-standard.md`.